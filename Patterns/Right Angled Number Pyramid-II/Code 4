let n = prompt("Enter the number of row: ");
let a=0; //Just declare a new variable and intialize it to 0.
for(let i=0; i<= n; i++){
    let star = '';
    for(let j=1; j<=i; j++){
        star += a;
    }
    a+=1; //After each row the value of 'a' increases to 1
    console.log(star);
}
