let n= prompt("Enter the number of rows: ");
for(let i=1; i<=n; i++){
    let star = '';
    //For the space
    for(let j=n; j>i; j--){
        star += ' ';
    }
    //For the stars
    for(let k=1; k<=i; k++){
        star += '* ';
    }
    console.log(star);
}
