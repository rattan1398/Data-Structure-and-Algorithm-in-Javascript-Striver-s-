let n = prompt("Enter the number of row: ");
for(let i=0; i<= n; i++){
    let star = '';
    for(let j=0; j<i; j++){ //The endpoint for the column is set according to row.
        star += '* ';
    }console.log(star);
}
