let n = prompt("Enter the number of row: ");
for(let i=0; i<= n; i++){
    let star = '';
    for(let j=0; j<i; j++){ //Endpoint for coulumn is based on the row's updation.
        star += '* ';
    }console.log(star);
}
