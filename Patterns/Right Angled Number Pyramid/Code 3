let n = prompt("Enter the number of row: ");
for(let i=0; i<= n; i++){
    let star = '';
    for(let j=1; j<=i; j++){ //Here we play with the column and print column based on the index of number to get the output.
        star += j;
    }
    console.log(star);
}
