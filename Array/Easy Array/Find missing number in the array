function missing(array){
    let sum=0;
    for(let i=0; i<array.length; i++){
        sum = sum+array[i];
    }
    let n = array.length;
    let expectedSum = (n* (n + 1))/2;
    return expectedSum - sum;
}
