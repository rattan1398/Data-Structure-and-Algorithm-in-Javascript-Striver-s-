function zero(array){

    let i=0;
    let temp =[];
    let arr = [];
    while(i<array.length){
        if(array[i] !== 0){
            temp.push(array[i]);
            i++;
        }
        else{
            arr.push(array[i]);
            i++
        }
    }return temp.concat(arr);
}
